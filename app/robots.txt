export default function robots() {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/api/', '/admin/', '/private/'],
    },
    sitemap: `https://${process.env.VERCEL_URL ?? 'business-sathi.vercel.app'}/sitemap.xml`,
    host: `https://${process.env.VERCEL_URL ?? 'business-sathi.vercel.app'}`,
  }
}
